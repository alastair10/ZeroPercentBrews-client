// can expand this to take name of badge?
import badgeKegImage from '../../images/badgeKegImage';

const BadgeKeg = () => {
  return <img src={badgeKegImage} alt='beer badge' />;
};

export default BadgeKeg;
